{
  "name": "aidlc-shared",
  "version": "0.1.0",
  "description": "Shared library for AI-DLC Kiro extensions — chat integration, webview panels, tree providers, file watching",
  "main": "./out/index.js",
  "types": "./out/index.d.ts",
  "files": ["out/**/*", "package.json"],
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
